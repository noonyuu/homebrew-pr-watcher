# homebrew-pr-watcher

[PR Watcher](https://github.com/noonyuu/PRWatcher) 用の Homebrew tap です。

## インストール

```bash
brew tap noonyuu/pr-watcher
brew install --cask pr-watcher
```

## アップデート

```bash
brew update
brew upgrade --cask pr-watcher
```
